# Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 21
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.

`````# Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 17+
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

```markdown
# Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 21
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.

## Endpoints

### PaisController
- **GET** `/api/pais`  
  Devuelve la lista de todos los países.
- **GET** `/api/pais/{id}`  
  Devuelve un país por su ID.
- **POST** `/api/pais`  
  Crea un nuevo país.
- **PUT** `/api/pais`  
  Actualiza un país existente.
- **DELETE** `/api/pais/{id}`  
  Elimina un país por su ID.

<!-- Agrega aquí secciones similares para los demás controladores que existan en tu proyecto --># Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 21
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.

## Endpoints

### PaisController
- **GET** `/api/pais`  
  Devuelve la lista de todos los países.
- **GET** `/api/pais/{id}`  
  Devuelve un país por su ID.
- **POST** `/api/pais`  
  Crea un nuevo país.
- **PUT** `/api/pais`  
  Actualiza un país existente.
- **DELETE** `/api/pais/{id}`  
  Elimina un país por su ID.

```markdown
# Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 21
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.

## Endpoints

### PaisController
- **GET** `/api/pais`  
  Devuelve la lista de todos los países.
- **GET** `/api/pais/{id}`  
  Devuelve un país por su ID.
- **POST** `/api/pais`  
  Crea un nuevo país.
- **PUT** `/api/pais`  
  Actualiza un país existente.
- **DELETE** `/api/pais/{id}`  
  Elimina un país por su ID.

<!-- Agrega aquí secciones similares para los demás controladores que existan en tu proyecto --># Ejerc5

Este proyecto contiene un ejemplo de aplicación con Spring Boot y Maven.

## Requisitos

- Java 21
- Maven 3.8+
- Spring Boot 3.4.2

## Estructura

- `pom.xml`: Configuración de Maven.
- `HELP.md`: Guía de referencia para iniciar el proyecto.

## Cómo ejecutar

1. Clonar el repositorio en tu máquina local.
2. Ejecutar el comando:
   ```bash
   mvn spring-boot:run
   ```
3. Abrir [http://localhost:8080](http://localhost:8080) en tu navegador.

## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.

## Endpoints

### Persona
- **GET** `/api/persona`  
  Devuelve la lista de todos las personas.
- **GET** `/api/persona/{id}`  
  Devuelve un país por su ID.
- **POST** `/api/persona`  
  Crea un nuevo persona.
- **PUT** `/api/persona`  
  Actualiza una persona existente.
- **DELETE** `/api/persona/{id}`  
  Elimina un persona por su ID.

### Provincia
- **GET** `/api/provincia`  
  Devuelve la lista de todos las provincias.
- **GET** `/api/provincia/{id}`  
  Devuelve una provincia por su ID.
- **POST** `/api/provincia`  
  Crea un nuevo provincia.
- **PUT** `/api/provincia`  
  Actualiza una provincia existente.
- **DELETE** `/api/provincia/{id}`  
  Elimina un provincia por su ID.

### Pais
- **GET** `/api/pais`  
  Devuelve la lista de todos los países.
- **GET** `/api/pais/{id}`  
  Devuelve un país por su ID.
- **POST** `/api/pais`  
  Crea un nuevo país.
- **PUT** `/api/pais`  
  Actualiza un país existente.
- **DELETE** `/api/pais/{id}`  
  Elimina un país por su ID.

### Continente
- **GET** `/api/continente`  
  Devuelve la lista de todos los continentes.
- **GET** `/api/continente/{id}`  
  Devuelve un continente por su ID.
- **POST** `/api/continente`  
  Crea un nuevo continente.
- **PUT** `/api/continente`  
  Actualiza un continente existente.
- **DELETE** `/api/continente/{id}`  
  Elimina un continente por su ID.



## Contribución

1. Haz un fork del proyecto.
2. Crea una rama nueva para tu contribución.
3. Envía un pull request describiendo tus cambios.
